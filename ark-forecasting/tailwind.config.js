/** @type {import('tailwindcss').Config} */
const config = {
    theme: {
        container: {
            center: true,
            padding: '1rem',
        },
    },
    plugins: [],
};

module.exports = config;